# Memory-Augmented-Context-MAC-Language-Model
PyTorch implementation of Memory-Augmented Context (MAC) for text generation. Combines neural long-term memory with attention mechanisms. 18 layers, 80M parameters, trained on TinyStories. Inspired by Google's Titans research on hybrid memory architectures for language modeling.
